{
  "version": "1.4.19",
  "date": "2026-06-15",
  "notes": "Setup wizard confetti now fills the whole screen (was a static clump in the top-left — the canvas wasn't being sized to the viewport); retina-crisp, resize-aware, denser, with mixed shapes. Server now honors the PORT env var so it can run on a custom port. Packaging still excludes .env (security fix from 1.4.18).",
  "files": [
    "index.html",
    "server.js",
    "version.json"
  ]
}
