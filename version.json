{
  "version": "1.4.60",
  "date": "2026-06-17",
  "notes": "Build/dev script now reads the Pexels API key from the environment instead of a hardcoded value. The app itself never bundles API keys — each user enters their own Pexels/Pixabay keys on first run (saved locally in UserConfig.json), and those persist across updates.",
  "files": [
    "build-japanese-content.mjs",
    "version.json"
  ]
}
