{
  "version": "1.4.39",
  "date": "2026-06-16",
  "notes": "Timeline editor: moved the scene header to the top of the left Content rail. The scene number is now a plain label (the expand/collapse dropdown is gone — it made no sense in the editor) with the scene actions inline beneath it (Scene Draft, Restore, Duplicate, Up, Down, Delete), sitting above 'Content'. buildContentRail prepends this scene-info block; the original .seg-header is hidden inside .tl-editor, so the right properties column now goes straight to the clean inspector. The block is a thin UI driving the same handlers and re-indexes correctly when switching scenes.",
  "files": [
    "index.html",
    "version.json"
  ]
}
