{
  "version": "1.4.47",
  "date": "2026-06-17",
  "notes": "Timeline view (time view) UX pass — left content rail. (1) Scene number is now large/bold (19px) so it reads as the heading of the rail. (2) Delete affordances are red by default: the per-layer ✕ and the scene 🗑 Delete button (previously grey, only red on hover). (3) Removed the scene-level ↑ Up / ↓ Down reorder buttons from the Timeline rail — scene reordering still lives in the Scene Editor card and the bottom timeline (drag), so it doesn't belong here. (4) Layer list now stacks foreground→background like CapCut: main text + overlays on top, Background / media at the bottom (was media-first).",
  "files": [
    "index.html",
    "version.json"
  ]
}
