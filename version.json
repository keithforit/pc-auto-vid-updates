{
  "version": "1.4.18",
  "date": "2026-06-15",
  "notes": "SECURITY: packaging scripts now exclude .env (and .claude/.agents/.vscode) so developer API keys are never shipped in the distribution zip — this also restores the first-run setup wizard for fresh installs (the bundled .env was silently providing keys and skipping setup)",
  "files": [
    "package.json",
    "version.json"
  ]
}
